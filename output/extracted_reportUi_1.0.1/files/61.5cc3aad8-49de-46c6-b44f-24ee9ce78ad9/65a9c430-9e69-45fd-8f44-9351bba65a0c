<style>
/* MultiSelect Widget - Modern Carbon Design Preview Styles */

.multiselect-preview-container .multiselect-container {
  width: 100%;
  font-family: 'IBM Plex Sans', 'Helvetica Neue', Arial, sans-serif; /* @bpm-font-family-sans-serif */
  font-size: 14px; /* @bpm-font-size-base */
  position: relative;
}

.multiselect-preview-container .multiselect-wrapper {
  position: relative;
  width: 100%;
}

/* Input Wrapper - Clean flat design */
.multiselect-preview-container .multiselect-input-wrapper {
  display: flex;
  align-items: center;
  min-height: 40px;
  padding: 0 12px;
  background-color: #f4f4f4; /* Gray 10 */
  border: none;
  border-bottom: 1px solid #8d8d8d; /* Gray 50 */
  cursor: pointer;
}

.multiselect-preview-container .multiselect-input-wrapper:hover {
  background-color: #e8e8e8; /* Gray 20 hover */
}

/* Selected Items Display */
.multiselect-preview-container .multiselect-selected-items {
  flex: 1;
  display: flex;
  flex-wrap: wrap;
  gap: 4px;
  padding: 4px 0;
  min-height: 24px;
  align-items: center;
}

.multiselect-preview-container .multiselect-placeholder {
  color: #525252; /* Gray 70 */
  font-size: 14px; /* @bpm-font-size-base */
  line-height: 1.29;
}

/* Selected Item Tag - Modern pill design */
.multiselect-preview-container .multiselect-tag {
  display: inline-flex;
  align-items: center;
  gap: 4px;
  padding: 0 8px;
  height: 24px;
  background-color: #e0e0e0; /* Gray 20 */
  border-radius: 12px;
  font-size: 12px; /* @bpm-font-size-small */
  line-height: 1;
  color: #161616; /* @bpm-text-color */
  max-width: 200px;
}

.multiselect-preview-container .multiselect-tag-text {
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.multiselect-preview-container .multiselect-tag-remove {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 16px;
  height: 16px;
  padding: 0;
  background: rgba(255, 255, 255, 0.2);
  border: none;
  border-radius: 50%;
  cursor: pointer;
  color: #ffffff;
  transition: all 0.2s ease;
}

.multiselect-tag-remove:hover {
  background: rgba(255, 255, 255, 0.3);
}

.multiselect-tag-remove svg {
  width: 12px;
  height: 12px;
}

/* Toggle Button */
.multiselect-toggle {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 40px;
  height: 40px;
  padding: 0;
  background: none;
  border: none;
  border-radius: 4px;
  cursor: pointer;
  color: #0f62fe;
  transition: all 0.2s ease;
}

.multiselect-toggle:hover {
  background: rgba(15, 98, 254, 0.1);
}

.multiselect-icon {
  width: 16px;
  height: 16px;
}

/* Dropdown - Hidden in preview */
.multiselect-dropdown {
  display: none;
}

/* Helper Text */
.multiselect-helper-text {
  margin-top: 0.375rem;
  font-size: 0.875rem;
  line-height: 1.5;
  color: #6f6f6f;
}

/* Responsive Design */
@media (max-width: 672px) {
  .multiselect-tag {
    font-size: 0.875rem;
    max-width: 180px;
  }
}
</style>